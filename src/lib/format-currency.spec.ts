import { describe, it } from 'node:test';
// import { formatToUSD } from './format-currency';

describe('formatToUSD', () => {
    it('should format a positive number correctly', () => {
        // const result = formatToUSD(1000);
        // expect(result).toBe('$1,000.00');
    });

    it('should format a negative number correctly', () => {
        // const result = formatToUSD(-500);
        // expect(result).toBe('-$500.00');
    });

    it('should format zero correctly', () => {
        // const result = formatToUSD(0);
        // expect(result).toBe('$0.00');
    });

    it('should format a decimal number correctly', () => {
        // const result = formatToUSD(1234.56);
        // expect(result).toBe('$1,234.56');
    });
});